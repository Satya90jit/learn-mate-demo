import { PublicLayouts } from '@/layouts'
import React from 'react'

const login = () => {
  return (
    <PublicLayouts>
        <section className='flex justify-center items-center h-screen '>
        <div className="text-6xl font-semibold">login</div>
        </section>
    </PublicLayouts>
  )
}

export default login